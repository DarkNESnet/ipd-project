# IPD Product Decision Tools — temporary pre-launch landing

This repository exists exclusively to host the temporary public identification landing for **IPD Product Decision Tools** during pre-launch.

It does **not** contain the IPD CORE, the commercial vertical, datasets, provider credentials, or pipeline logic.

Rules for future agents and operators:

- Do not add secrets, tokens, passwords, API keys, private email addresses, billing data, internal ZIPs, project documents, commercial datasets, or private sources.
- Do not add IPD CORE code or data-ingestion / decision-pipeline logic here.
- Do not treat this repository as the commercial asset, SEO site, affiliate site, shop, or production application.
- Keep the repository minimal and static unless the project explicitly changes its purpose.

Current purpose: provide a stable public URL that identifies the project to external data providers while the real product remains in pre-launch development.
